export default function Leaderboard() {

  // router.get("/history", auth, getGameHistory);
  // router.get("/:id", auth, getGameById);

  return (
    <div>
      <h1 className="text-2xl font-bold text-pong-text">Leaderboard</h1>
    </div>
  );
}
